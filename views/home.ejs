<%- include("partials/header"); -%>

    <h1>HOME</h1>
    <p><%= startingContent %></p>
    <% console.log(posts); %>
    
    <% posts.forEach(function(post){ %>
       <!-- <% console.log(post.title); %> -->
       <h1><%= post.title %></h1>
       <p><%= post.content %></p>
    <% }) %>

<%- include("partials/footer"); -%>
